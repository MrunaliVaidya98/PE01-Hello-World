# Run the project

Open a terminal in this folder and run:

```bash
npm install
npx expo start --clear --lan
```

Use Expo Go on a phone connected to the same Wi-Fi network and scan the QR code.

If LAN mode is unavailable, try:

```bash
npx expo start --clear --tunnel
```

Do not run `npm audit fix --force` for this course exercise.
